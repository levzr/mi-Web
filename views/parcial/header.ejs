<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>PedidosHN</title>

  <!-- Fuente y estilos -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/styles.css">
</head>
<body>

  <!-- NAVBAR -->
  <nav class="navbar">
    <!-- Marca -->
    <a href="/" class="brand">
      <img src="/imgs/logo.png" alt="PedidosHN" width="40" height="40">
      <span class="brand-name">PedidosHN</span>
    </a>

    <!-- Links principales -->
    <div class="nav-links">
      <a href="/contacto" class="nav-link">Contactanos</a>

      <% if (user) { %>
        <a href="/pedidos" class="nav-link">Mis pedidos</a>
      <% } %>

      <% if (user && user.es_admin) { %>
        <a href="/admin/usuarios" class="nav-link">Usuarios(Admin)</a>
        <a href="/admin/contactos" class="nav-link">Mensajes(Admin)</a>
        <a href="/admin/pedidos" class="nav-link">Pedidos(Admin)</a>
        <a href="/admin/restaurantes" class="nav-link">Restaurantes(Admin)</a>
        <span style="margin-left:16px;color:#1abc9c;font-weight:600;">
          Usuario Admin
        </span>
      <% } %>
    </div>

    <!-- Login, register, logout -->
    <div class="nav-actions">
      <% if (user) { %>
        <span>👋 Hola, <%= user.nombre %></span>
        <form action="/api/logout" method="POST" style="display:inline;">
          <button type="submit" class="btn-ghost" style="border-radius:6px;">Cerrar sesión</button>
        </form>
      <% } else { %>
        <a href="/login"
           class="<%= currentPage === 'login' ? 'btn-primary' : 'btn-ghost' %>"
           style="border-radius:6px;">
          Iniciar sesión
        </a>
        <a href="/register"
           class="<%= currentPage === 'register' ? 'btn-primary' : 'btn-ghost' %>"
           style="border-radius:6px;">
          Registrarse
        </a>
      <% } %>
    </div>
  </nav>
