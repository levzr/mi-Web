<%- include('parcial/header', { siteName: siteName, currentYear: currentYear }) %>

<div class="container">
  <div class="panel success-panel">
    <h2>✅ Pedido confirmado</h2>
    <p>¡Gracias, <%= nombre %>!</p>
    <p>Tu orden fue registrada correctamente.</p>

    <h3>Resumen</h3>
    <p><strong>Restaurante:</strong> <%= restaurante || restaurante_slug || "Desconocido" %></p>
    <p><strong>Plato:</strong> <%= pedido %></p>
    <p><strong>Dirección:</strong> <%= direccion %></p>
    <p><strong>Fecha:</strong> <%= fecha %></p>

    <a href="/" class="btn-primary">Volver al inicio</a>
  </div>
</div>

<%- include('parcial/footer', { siteName: siteName, currentYear: currentYear }) %>

